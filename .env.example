# Fichier d'exemple de variables d'environnement.
# Copier ce fichier en `.env` et y renseigner les vraies valeurs (jamais commité).
# Les variables (connexion PostgreSQL, URI MLflow, clés API, etc.) seront
# ajoutées progressivement au fil des phases du projet.
